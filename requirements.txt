# Museum Kiosk - Python Dependencies
flask==3.0.0
gunicorn==21.2.0
pillow==10.1.0
mysql-connector-python==8.2.0

# Content loading (markdown + frontmatter)
PyYAML>=6.0
markdown>=3.5
python-frontmatter>=1.0.0

# Migration tools (already installed)
markdownify>=0.11.0
python-slugify>=8.0
